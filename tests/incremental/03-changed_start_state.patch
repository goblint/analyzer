--- a/tests/incremental/02-changed_start_state.c
+++ b/tests/incremental/02-changed_start_state.c
@@ -1,6 +1,6 @@
 #include <assert.h>

-int g = 1;
+int g = 2;

 int main() {
     // After the presolve phase, g is in the start state but neither in the context nor in the start variables.
