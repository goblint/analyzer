1. Download ocaml 4.00.1
./configure
make world.opt
sudo make install

2. get findlib 1.3.3 
./configure
make all
make opt
sudo make install

3. get camomile 0.8.3
./configure
make
sudo make install

4. get batteries 1.5.0 (!!!)
make 
sudo make install


5. get Cil
git clone git://cil.git.sourceforge.net/gitroot/cil/cil
./configure
make opt
sudo make install

6. get xml-light 2.2
make 
make opt
echo 'version="2.2"' > META
echo 'archive(byte)="xml-light.cma"' >> META
echo 'archive(native)="xml-light.cmxa"' >> META
sudo ocamlfind install xml-light META *.cmx *.cmi *.mli *.a *.o *.cmxa
